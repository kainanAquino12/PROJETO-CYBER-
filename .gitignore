# Dependencias
node_modules/
server/node_modules/

# Variaveis de ambiente (segredos / chaves)
# Obs: o .env.example E versionado de proposito (template, sem segredos reais).
.env
server/.env

# Dados coletados + credenciais do painel (privacidade — NAO versionar).
# Inclui visits.json e auth.json (hashes de senha + segredo dos tokens).
server/data/

# Logs / temporarios
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# Config local do front (mantenha config.example.js versionado)
# Descomente se quiser nao versionar a URL real da sua API:
# config.js

# Editor
.vscode/
